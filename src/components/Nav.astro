<nav aria-label="Primary navigation">
    <a href="#Experience"><span data-i18n="navExperience">Experience</span></a>
    <a href="#Projectsx"><span data-i18n="navProjects">Projects</span></a>
    <a href="#Skills"><span data-i18n="navSkills">Skills</span></a>
    <a href="#AboutMe"><span data-i18n="navAbout">About me</span></a>
</nav>

<script>
    const links = [...document.querySelectorAll("nav a")];
    const sections = links.map((link) => document.querySelector(link.getAttribute("href"))).filter(Boolean);
    const updateActive = () => {
        const marker = window.scrollY + window.innerHeight * 0.28;
        let active = sections[0];
        for (const section of sections) if (section.offsetTop <= marker) active = section;
        links.forEach((link) => {
            const isActive = link.getAttribute("href") === "#" + active.id;
            link.toggleAttribute("data-active", isActive);
            if (isActive) link.setAttribute("aria-current", "page"); else link.removeAttribute("aria-current");
        });
    };
    updateActive();
    window.addEventListener("scroll", updateActive, { passive: true });
</script>

<style>
    nav {
        width: min(720px, calc(100% - 28px));
        min-height: 52px;
        position: fixed;
        top: 14px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        gap: 2px;
        padding: 4px 8px;
        background: rgba(16,16,23,.72);
        border: 1px solid rgba(255,255,255,.1);
        border-radius: 16px;
        z-index: 99999999;
        box-shadow: 0 14px 40px rgba(0,0,0,.28);
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
    }
    a {
        flex: 1 1 0;
        min-width: 0;
        text-align: center;
        text-decoration: none;
        color: #a9a9b4;
        padding: 9px 10px;
        border-radius: 11px;
        transition: color .2s ease, background-color .2s ease, transform .2s ease;
    }
    span { border-bottom: 1px solid transparent; transition: border-color .2s ease; white-space: nowrap; }
    a:hover, a[data-active] { color: #fff; background: rgba(127,90,240,.13); }
    a:hover { transform: translateY(-1px); }
    a[data-active] span { border-bottom-color: #a78bfa; }
    @media (max-width: 850px) {
        nav { width: calc(100% - 16px); top: 8px; min-height: 46px; padding: 3px 4px; border-radius: 14px; }
        a { padding: 8px 4px; font-size: clamp(.68rem, 2.7vw, .8rem); }
    }
    @media (max-width: 390px) {
        nav { width: calc(100% - 10px); }
        a { padding-inline: 2px; font-size: .66rem; }
    }
    @media (prefers-reduced-motion: reduce) { a { transition: none; } }
</style>

<style is:global>
    @media (min-width: 851px) {
        main {
            padding: 20px 0;
            box-sizing: border-box;
        }

        .Me {
            width: min(82%, 1180px);
            height: 72%;
            gap: clamp(48px, 6vw, 96px);
            justify-content: space-between;
            overflow: visible;
        }

        .Me img {
            width: clamp(250px, 22vw, 310px);
            height: clamp(250px, 22vw, 310px);
            flex: 0 0 auto;
        }

        .Me > div {
            width: auto;
            flex: 1 1 auto;
            min-width: 0;
            max-width: 760px;
        }

        .Me h1 {
            margin: 0 0 18px;
            font-size: clamp(2.45rem, 3.65vw, 3.65rem);
            line-height: 1.08;
            letter-spacing: -0.025em;
            white-space: nowrap;
        }

        .eyebrow {
            margin-bottom: 14px;
        }

        .hero-copy {
            max-width: 58ch;
            line-height: 1.65;
            margin: 0;
        }

        .hero-actions {
            margin-top: 26px;
            gap: 10px;
        }

        .social-button {
            margin: 4px 2px;
            padding: 9px 17px;
        }
    }

    @media (min-width: 851px) and (max-width: 1050px) {
        .Me {
            width: 88%;
            gap: 40px;
        }

        .Me h1 {
            font-size: clamp(2.25rem, 3.7vw, 3rem);
        }
    }
</style>